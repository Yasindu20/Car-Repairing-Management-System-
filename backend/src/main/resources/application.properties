spring.jpa.show-sql=true

spring.datasource.url=jdbc:mysql://localhost:3306/carmanagement?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Yasindu20$
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Updated database dialect for Spring Boot 3 and MySQL 8
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# Server port
server.port=8080

# Logging levels for debugging
logging.level.org.springframework.web=INFO
logging.level.com.example.computer_item_repair=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Remove deprecated properties
# spring.main.allow-bean-definition-overriding=true

# Disable debug mode for production
debug=false

# Jackson configuration for proper date handling
spring.jackson.date-format=yyyy-MM-dd
spring.jackson.time-zone=UTC